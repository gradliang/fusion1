#include <stdio.h>
#include <stdlib.h>

#define MPX_NONCACHE(a)    (a)
#define AVIENDIAN_PAD(a)   (a) 
#define mpDebugPrint(a)    printf(a)

typedef unsigned int DWORD;
typedef unsigned char BYTE;
typedef FILE STREAM;
