#ifndef WAV_UTIL_H_#define WAV_UTIL_H_#include "UtilTypeDef.h"typedef struct{	DWORD chunkId;	DWORD chunkSize;	DWORD format;	DWORD subchunkId1;	DWORD subchunkId1size;	WORD  audioFormat;	WORD  numChannel;	DWORD sampleRate;	DWORD byteRate;	WORD  blockAlign;	WORD  bitPerSample;	DWORD subchunkId2;	DWORD subchunkId2size;}WAV_HEADER;typedef struct{	WORD  Channels;	DWORD SampleRate;	DWORD AvgBytesPerSec;	WORD  BlockAlign;			// BlockAline = Channels * (BitsPerSample / 8)	WORD  BitsPerSample;	DWORD DataOffset;	DWORD DataSize;} WAV_CONFIG;#define MGET_DWORD(x,y)	(y)=*(x+3);(y)=(y<<8)+*(x+2);(y)=(y<<8)+*(x+1);(y)=(y<<8)+*(x);(x)+=4#define MGET_WORD(x,y)	(y)=*(x+1);(y)=(y<<8)+*(x);(x)+=2#define MGET_SKIPBYTE(x,y) (x) += (y)	#endif	